with-expecter: true
filename: "{{.InterfaceName}}.go"
dir: "{{.InterfaceDir}}/mocks"
mockname: "{{.InterfaceName}}"
outpkg: "mocks"
packages:
  github.com/clambin/tado-exporter/controller:
    interfaces:
      TadoSetter:
  github.com/clambin/tado-exporter/controller/commands:
    interfaces:
      TadoSetter:
      Controllers:
  github.com/clambin/tado-exporter/controller/slackbot:
    interfaces:
      SlackBot:
  github.com/clambin/tado-exporter/controller/zone/rules:
    interfaces:
      TadoSetter:
  github.com/clambin/tado-exporter/internal/collector:
    interfaces:
      TadoSetter:
  github.com/clambin/tado-exporter/internal/poller:
    interfaces:
      Poller:
      TadoGetter:
