name: Test

on:
  push:
    branches-ignore:
      - master
      - refactor
      - metrics
  pull_request_target:

jobs:
  test:
    uses: clambin/workflows/.github/workflows/test.yaml@main
    with:
      go-version: 1.17
